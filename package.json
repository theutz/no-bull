{
  "private": true,
  "name": "no-bull-packages",
  "husky": {
    "hooks": {
      "pre-commit": "pretty-quick --staged"
    }
  },
  "scripts": {
    "start": "cd website && npm run develop",
    "build": "node ./scripts/build.js",
    "clean": "lerna run clean",
    "test": "lerna run test --stream"
  },
  "devDependencies": {
    "husky": "^0.14.3",
    "lerna": "^2.9.1",
    "prettier": "^1.11.1",
    "pretty-quick": "^1.4.1"
  }
}
